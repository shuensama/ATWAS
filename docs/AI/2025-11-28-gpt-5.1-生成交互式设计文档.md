# Speckit interaction command
_Exported on 11/28/2025 at 22:41:42 GMT+8 from Cursor (2.1.39)_

---

**User**

/speckit.interaction 

--- Cursor Command: speckit.interaction.md ---
---
description: Generate an interaction-flow and low-fidelity prototype design from the current feature specification.
handoffs: 
  - label: Build Technical Plan
    agent: speckit.plan
    prompt: Create a technical plan that respects both the spec and the interaction design. I am building with...
    send: true
---

## User Input

```text
$ARGUMENTS
```

You **MUST** consider the user input before proceeding (if not empty).

## Goal

Bridge the gap between a text-only spec and implementation by generating a **fully spec‑aligned
interaction-flow design** plus a **lightweight, functional prototype sketch**. This command sits
between `/speckit.specify` (and `/speckit.clarify`) and `/speckit.plan`:

- Translate `spec.md` into explicit, end‑to‑end user flows and screens
- Make implicit mental models visible and reviewable before technical planning
- Surface remaining ambiguity as concrete questions tied to steps/screens
- Produce a simple, technology‑agnostic prototype description for key screens

This step MUST NOT introduce new hidden requirements; any necessary assumptions MUST be
called out explicitly for later spec updates or clarification.

## Outline

1. **Setup**: Locate the active feature and its `spec.md`
2. **Load spec context**: Extract actors, user stories, requirements, and edge cases
3. **Build interaction model**: Derive user flows, screens, and states strictly from the spec
4. **Generate interaction-design.md**: Document flows, screens, and open questions
5. **Generate low-fidelity prototypes**: Sketch key screens in a concise, implementation‑agnostic way
6. **Coverage & ambiguity summary**: Ensure every user story is covered and gaps are visible
7. **Next actions**: Suggest whether to refine the spec further or proceed to `/speckit.plan`

## Execution Steps

### 1. Setup (feature discovery)

1. Run `.specify/scripts/powershell/check-prerequisites.ps1 -Json -PathsOnly` from the repo root.
2. Parse the minimal JSON payload fields:
   - `FEATURE_DIR`
   - `FEATURE_SPEC` (path to `spec.md`)
   - (Optionally capture `IMPL_PLAN`, `TASKS` for future chained flows.)
3. If JSON parsing fails or `FEATURE_SPEC` is missing:
   - Abort and instruct the user to run `/speckit.specify` first (and `/speckit.clarify` if needed).
4. For single quotes in args like "I'm Groot", use escape syntax:
   - PowerShell: `"I'm Groot"` or `'I'\''m Groot'`

### 2. Load spec context (what we design from)

1. Read `FEATURE_SPEC` once into memory; do not modify it in this command.
2. Extract the following sections (using the spec template structure as guidance):
   - Overview / Context
   - User Roles / Personas (if present)
   - User Stories (including priorities such as P1, P2, P3)
   - Functional Requirements
   - Non‑Functional Requirements related to UX, performance, and accessibility
   - Edge Cases / Error Handling (if present)
3. Build a **user story inventory**:
   - Stable ID (e.g., `US1`, `US2`, or existing IDs in the spec)
   - Title / short label
   - Priority (P1/P2/…)
   - Acceptance criteria (if provided)
4. Build an **actor inventory**:
   - Distinct user roles (e.g., Anonymous User, Authenticated User, Admin)
   - System / background actors (e.g., Scheduler, External API) if referenced in the spec

### 3. Build interaction model (flows, screens, states)

1. For each user story in the inventory:
   - Identify:
     - Entry conditions (preconditions, triggers, starting screen/state)
     - Success criteria (what “done” looks like for the user)
   - Draft a **Main Success Flow**:
     - A linear sequence of steps from entry to success:
       - At each step, record:
         - **Actor action** (what the user does or what event occurs)
         - **System response** (visible outcome, not implementation detail)
         - **Screen / state** (where this happens)
         - References to relevant spec sections (e.g., `[Spec §US1]`, `[Spec §FR-3]`)
   - Draft **Alternate / Exception Flows** where indicated by the spec:
     - Alternate choices, optional branches, or role‑specific variations
     - Error, empty, or edge‑case paths (e.g., validation errors, missing data)
2. Normalize **screens / views** from the flows:
   - Cluster steps into logical screens (e.g., "Landing Page", "Episode Detail", "Settings Dialog")
   - Assign each screen a stable ID: `S1`, `S2`, `S3`, …
   - For each screen, record:
     - Name and short purpose
     - Which user stories and flows use it
     - Key states (e.g., empty, loading, error, populated)
3. Guardrails for alignment with `spec.md`:
   - Do NOT invent new features or behaviour not implied by the spec.
   - When a design decision requires an assumption (e.g., default sorting, pagination style):
     - Mark it as `[ASSUMPTION]` and reference the related story or requirement.
   - When a flow step reveals a gap or contradiction in the spec:
     - Mark it as `[NEEDS SPEC UPDATE]` and quote the relevant spec text where possible.

### 4. Generate interaction-design.md (flows & screens)

1. Target output path: `${FEATURE_DIR}/interaction-design.md`.
2. This file SHOULD be **idempotent**:
   - Overwrite or regenerate the entire file each run based on the current `spec.md`.
3. Use the following high‑level structure (do not embed raw spec text; summarize instead):

   - `# Interaction & Prototype Design: [FEATURE NAME]`
   - **Source of Truth**
     - Path to `spec.md`
     - Version or commit reference if available
   - **Actors & Roles**
     - Bullet list of actors with short responsibilities
   - **Screen Inventory**
     - Markdown table:

       ```text
       | Screen ID | Name              | Used By Stories | Key States                |
       |-----------|-------------------|-----------------|---------------------------|
       | S1        | Landing Page      | US1, US2        | empty, loading, populated |
       | S2        | Episode Detail    | US1             | loading, error, success   |
       ```

   - **Flows by User Story**
     - For each user story (in priority order, P1 first):
       - `### [USx] Title (Priority: Px)`
       - `#### Main Success Flow`
         - Numbered steps: `<Step N> [Actor] → [System] @ [ScreenID] (Spec refs)`
       - `#### Alternate / Exception Flows` (if any)
         - Bulleted sub‑flows with branching conditions
   - **Open Questions, Assumptions & Spec Gaps**
     - Grouped list of:
       - `[ASSUMPTION] ... (Story: USx, Screen: Sy)`
       - `[NEEDS SPEC UPDATE] ... (Story: USx, Flow step N, Spec §ref)`

4. Keep this document **technology‑agnostic**:
   - No mention of frameworks, routes, components, or styling systems.
   - Focus on **what the user sees and does**, and **how the system responds**.

### 5. Generate low-fidelity prototypes (within interaction-design.md)

1. In the same `interaction-design.md`, add a `## Low-Fidelity Prototypes` section after the flows.
2. For each screen in the Screen Inventory (especially those participating in P1 flows):
   - Add a subsection: `### [ScreenID] [Screen Name]`
   - Include:
     - **Purpose**: 1–2 sentences linking to user stories and flows.
     - **Priority**: e.g., `P1 Smoke`, `P2`, `Support`.
     - **Layout outline**:
       - Main regions (Header, Sidebar, Content, Footer, Modal, etc.)
       - For each region: short bullet describing what it contains conceptually.
     - **Key interactive elements**:
       - Labels and roles (e.g., "Primary CTA: Start Episode", "Filter dropdown")
       - Important states (default, disabled, error, loading).
   - Optionally, render a simple ASCII wireframe in a fenced code block to make the layout
     more concrete, for example:

     ```text
     +--------------------------------------------------+
     | Header: Title + Primary CTA                      |
     +---------------------+----------------------------+
     | Sidebar: Filters    | Content: Episode list      |
     |                     | - Card with title, badge   |
     |                     | - Empty state placeholder  |
     +---------------------+----------------------------+
     ```

3. Do NOT attempt high‑fidelity visual design:
   - No pixel specs, colours, fonts, or spacing systems.
   - The goal is to make the **structure and interaction** obvious to both PMs and engineers.

### 6. Coverage & ambiguity summary

1. At the end of `interaction-design.md`, add a `## Coverage Summary` section containing:

   - A coverage table:

     ```text
     | User Story ID | Priority | Has Main Flow? | Has Alt/Exception? | Screens Used | Notes |
     |---------------|----------|----------------|--------------------|--------------|-------|
     | US1           | P1       | Yes            | Yes                | S1, S2       |       |
     | US2           | P2       | Yes            | No                 | S1           | [ASSUMPTION] X |
     ```

   - Metrics:
     - Total user stories
     - Stories with ≥1 main flow
     - Stories with alt/exception flows
     - Count of `[ASSUMPTION]` items
     - Count of `[NEEDS SPEC UPDATE]` items
2. Assign a simple severity heuristic:
   - **CRITICAL**:
     - Any P1 story without a main success flow
     - Any contradiction between flows and spec that blocks understanding
   - **HIGH**:
     - Stories with flows that depend on multiple `[NEEDS SPEC UPDATE]` items
   - **MEDIUM**:
     - Stories missing explicit edge‑case / error flows despite risky operations
   - **LOW**:
     - Stylistic decisions or UI polish aspects clearly deferrable to implementation

### 7. Next actions & integration with other commands

1. This command is **read‑only** with respect to `spec.md`:
   - It MUST NOT modify the spec directly.
   - Instead, it documents where the spec should be updated or clarified.
2. At the end of the run, report to the user:
   - Path to `interaction-design.md`
   - Coverage metrics (stories covered, critical gaps)
   - Counts of `[ASSUMPTION]` and `[NEEDS SPEC UPDATE]` items
3. Suggest next steps based on results:
   - If any **CRITICAL** or **HIGH** issues exist:
     - Recommend updating the spec and/or running `/speckit.clarify` to resolve gaps
     - Advise against proceeding to `/speckit.plan` until these are addressed
   - If only **MEDIUM/LOW** issues exist:
     - Recommend optionally tightening the spec, then proceeding to `/speckit.plan`
4. When `/speckit.plan` is invoked afterwards:
   - Treat `interaction-design.md` as an **additional, non‑authoritative input**:
     - It MAY guide API design, data modelling, and contracts.
     - In any conflict, `spec.md` and the constitution remain the sources of truth.

## Operating Principles

- **Spec alignment first**: All flows and prototypes must map back to existing stories and
  requirements; gaps are called out, not silently filled.
- **Visualization over narration**: Prefer structured flows, tables, and simple diagrams over
  long prose to reduce ambiguity.
- **Technology agnostic**: No framework or implementation details; this is a UX/interaction
  design layer, not a code‑level plan.
- **Review‑friendly**: `interaction-design.md` should be easy for both PMs and engineers to
  review and comment on before deep technical planning begins.



--- End Command ---

---

**Cursor**

我会先运行项目自带的检查脚本拿到当前 feature 的目录和 `spec.md` 路径，然后读取规范内容，整理出用户故事、角色和需求，再据此设计用户流程与界面清单，最后生成/更新 `interaction-design.md` 文件（包含流程、低保真原型和覆盖摘要）。整个过程只读 `spec.md`，不擅自引入新需求，所有假设与规范缺口都会在文档中明确标注。

我已经拿到 feature 的目录和 `spec.md` 路径，接下来会读取规范并开始整理用户故事与流程。

我已经完整读取了 `spec.md` 和 `tasks.md`，现在会基于这两者整理用户故事、界面与交互流并生成 `interaction-design.md`。

我现在会直接在 `specs/001-ai-drama-stage/` 下创建 `interaction-design.md`，其中包含屏幕表、按用户故事组织的主/备选流程、低保真原型以及覆盖与缺口总结。

```markdown
# Interaction & Prototype Design: AI情景剧舞台（多 AI 角色轮流对话）

## Source of Truth

- **Spec path**: `specs/001-ai-drama-stage/spec.md`
- **Feature branch**: `001-ai-drama-stage`
- **Related docs (read-only)**: `specs/001-ai-drama-stage/plan.md`, `specs/001-ai-drama-stage/tasks.md`, `specs/001-ai-drama-stage/data-model.md`, `specs/001-ai-drama-stage/contracts/drama-stage.openapi.yaml`, `specs/001-ai-drama-stage/quickstart.md`（仅作参考，行为以 `spec.md` 为准）

## Actors & Roles

- **Authenticated User（创作者/普通用户）**: 配置场景与角色、启动和控制情景剧演出、查看与管理模板库与历史演出记录。（[Spec §User Story 1–3]）
- **System / Drama Orchestrator**: 根据会话配置（场景、角色、轮流顺序、总轮数）生成每一轮对话，管理会话状态（进行中、已完成、可继续）并持久化 Turn 历史。（[Spec §FR-001–FR-003], [Spec §FR-011–FR-013]）
- **AI Model**: 生成情景剧对话内容与角色设定草稿，遵守安全与合规约束，对明显违规或不适宜内容进行拦截或弱化处理。（[Spec §FR-006], [Spec §FR-010]）

## Screen Inventory

| Screen ID | Name                             | Used By Stories | Key States                                                                 |
|-----------|----------------------------------|-----------------|----------------------------------------------------------------------------|
| S1        | 情景剧创建与演出主界面          | US1, US2, US3   | 初次进入/空配置, 配置进行中, 校验错误, 演出进行中, 暂停, 已完成, 恢复中, 错误 |
| S2        | 创建/编辑角色（含 AI 生成）界面 | US2, US1        | 输入描述, 生成中, 草稿可编辑, 保存成功, 生成失败/安全拦截                     |
| S3        | 模板库（场景 & 角色）界面       | US3, US1, US2   | 列表加载中, 空列表, 有结果, 搜索/筛选中, 编辑中, 删除确认                     |
| S4        | 历史演出列表界面                | US1, US3        | 列表加载中, 空历史, 有结果, 过滤/排序中, 删除确认                             |
| S5        | 历史演出详情 / 回看 & 继续界面   | US1, US3        | 对话加载中, 已完成可回看, 进行中可继续, 加载失败, 一键复制成功/失败          |

## Flows by User Story

### [US1] 创建并观看一场多 AI 情景剧 (Priority: P1)

#### Main Success Flow（从零配置一场演出并观看至结束）

1. [用户] 打开 AI 情景剧功能入口 → [系统] 显示空白的情景剧创建与演出主界面，包含「场景配置区」「角色配置区」「演出控制区」和提示需至少配置 1 个场景与 2 个角色。 @ S1 ([Spec §User Story 1], [Spec §FR-001], [Spec §Dependencies & Constraints])
2. [用户] 在「场景配置区」选择「新建场景」，输入或粘贴场景 prompt，并填写场景名称等基础信息 → [系统] 校验必填项合法性，通过后展示该场景的概要信息，并在界面上标记为当前会话将使用的场景。 @ S1 ([Spec §User Story 1], [Spec §FR-001], [Spec §FR-004])
3. [用户] 在「角色配置区」依次添加至少 2 个 AI 演员，为每个演员填写名称、角色 prompt，可选上传或选择头像/标识 → [系统] 限制角色数量在 2–6 之间，对缺失名称或 prompt 的角色给出校验提示，并在通过校验后展示角色列表预览。 @ S1 ([Spec §User Story 1], [Spec §FR-001], [Spec §Key Entities: RoleTemplate])
4. [用户] 配置轮流顺序（每轮发言的角色顺序）和预计总轮数（例如 5 轮），确认本场演出的基础参数 → [系统] 在界面显著位置展示当前会话的配置摘要（场景 + 角色 + 顺序 + 总轮数），供用户复核。 @ S1 ([Spec §User Story 1], [Spec §FR-002], [Spec §FR-008])
5. [用户] 点击「开始演出」 → [系统] 基于当前配置创建一个新的 DramaSession，会话中保存场景与角色快照以及配置参数，并将界面切换到「演出进行中」状态：上方显示会话信息，中部为多角色轮流发言视图，下方为控制区。 @ S1 ([Spec §User Story 1 Acceptance 1], [Spec §FR-001–FR-003], [Spec §FR-008–FR-009], [Spec §Key Entities: DramaSession])
6. [系统] 根据预设轮流顺序自动触发第 1 轮 Turn：调用 AI Model 基于场景和当前轮次上下文生成第一个发言 → [系统] 在对话区域以清晰的身份标识（包含角色名称与头像）展示生成结果，并标注为「第 1 轮」。 @ S1 ([Spec §User Story 1], [Spec §FR-002], [Spec §Key Entities: Turn])
7. [系统] 按轮流顺序为每一轮触发后续发言，直至达到用户配置的总轮数：每一轮均基于当前会话状态与前序 Turn 内容生成 → [系统] 在同一界面中追加展示每一轮发言，以时间线/对话气泡方式清晰区分角色与轮次。 @ S1 ([Spec §User Story 1], [Spec §FR-002], [Spec §Dependencies & Constraints: UI 展示])
8. [系统] 在生成每一轮对话的同时，将对应 Turn 记录写入存储，与 DramaSession 关联，以便后续在历史列表和详情中逐轮回看 → [系统] 在合适位置提示用户「本场演出将被保存，可在历史中回看」。 @ S1 ([Spec §FR-011], [Spec §Clarifications: 历史演出记录], [Spec §Key Entities: Turn])
9. [系统] 当达到预设总轮数时，自动停止继续生成后续轮次，将会话状态标记为「已完成」，在界面上明确提示本场演出已结束，并允许用户选择「回看本场演出详情」或「返回配置新演出」。 @ S1 ([Spec §User Story 1 Acceptance 1], [Spec §FR-011])
10. [用户] 选择查看历史详情或稍后回看 → [系统] 在历史演出列表中可见该次 DramaSession 记录，并可在详情中按轮次回看完整对话内容。 @ S4/S5 ([Spec §Clarifications: 历史演出详情页逐轮回看], [Spec §FR-011–FR-013])

#### Alternate / Exception Flows

- **US1-A1：从最近使用/收藏快速开场（加速创建）**  
  - 条件：用户已经保存过若干场景和角色。 ([Spec §User Story 1 Acceptance 2], [Spec §FR-004–FR-005], [Spec §FR-007–FR-009])  
  - A1.1 在主流程步骤 2 之前，[用户] 在场景区域打开「最近使用/收藏」列表，从中选取一个场景模板 → [系统] 自动填充场景配置区并展示摘要。 @ S1  
  - A1.2 在主流程步骤 3 之前，[用户] 在角色区域从「最近使用/收藏」中勾选至少 2 个角色模板 → [系统] 自动填充角色列表，可供进一步编辑名称或 prompt。 @ S1  
  - A1.3 [系统] 在用户完成选择后更新会话配置摘要，并在用户点击「开始演出」后直接进入主流程步骤 5–10；从进入创建到看到第一轮对话的整体用时目标不超过 3 分钟。 @ S1 ([Spec §User Story 1 Acceptance 2])

- **US1-A2：播放控制（暂停 / 继续 / 手动下一轮 / 提前结束）**  
  - 条件：一场演出正在进行中。 ([Spec §User Story 1 Acceptance 3], [Spec §FR-003])  
  - A2.1 在主流程步骤 6–7 任意时刻，[用户] 点击「暂停」 → [系统] 在当前轮次生成完成后停止自动触发下一轮，将会话状态更新为「暂停」，界面上突出暂停状态，并允许用户稍后点击「继续」或「结束本场」。 @ S1  
  - A2.2 [用户] 在暂停或进行中状态点击「下一轮」 → [系统] 仅在用户点击时触发下一轮 Turn 生成，而非自动连续生成，直到再次点击或达到总轮数。 @ S1  
  - A2.3 [用户] 在任意时刻点击「结束本场」 → [系统] 在当前轮次生成完成后标记会话为「已结束」，不再允许生成新的 Turn，同时保留已生成的所有历史轮次以供回看。 @ S1

- **US1-A3：角色 prompt 过于简短或含糊时的提示**  
  - 条件：用户仅配置了 2 个角色，其中至少一个角色的 prompt 极其简短或含糊。 ([Spec §Edge Cases 第 1 条])  
  - A3.1 在主流程步骤 3 或 4，[系统] 通过简单启发式检测到某些角色 prompt 过短或过于笼统 → [系统] 允许继续配置与启动演出，但在角色配置区或摘要区域显示提示，例如「当前角色设定较为简略，可适当补充以提升演出效果」。 @ S1  
  - A3.2 [用户] 选择忽略提示继续演出 → [系统] 仍按主流程 5–10 正常生成多轮对话。 @ S1

- **US1-A4：角色数量或轮数超过推荐上限**  
  - 条件：用户在配置中尝试添加超过 6 个角色，或设置超过 20 轮的总轮数。 ([Spec §Edge Cases 第 3 条], [Spec §FR-001])  
  - A4.1 在主流程步骤 3 或 4，[用户] 继续添加第 7 个角色或将轮数设置为大于 20 → [系统] 在界面上明确提示「超过推荐上限可能影响体验」，说明可能导致生成时间变长或信息过载。 @ S1  
  - A4.2 [系统] 要求用户明确确认（例如通过确认对话框或复选框）后才能继续当前配置；如用户取消，则将角色数量或轮数回退到推荐上限。 @ S1

- **US1-A5：某轮输出异常冗长或明显偏离角色设定**  
  - 条件：某一轮 AI 输出远超预期长度或明显不符合该角色设定。 ([Spec §Edge Cases 第 2 条], [Spec §FR-010])  
  - A5.1 在主流程步骤 7 某轮生成完成后，[用户] 认为内容冗长或偏离设定 → [系统] 在该轮旁提供「跳过本轮」「重新生成本轮」「结束演出」等操作入口。 @ S1  
  - A5.2 [用户] 选择「跳过本轮」 → [系统] 在 UI 中弱化显示该轮（例如标记为已跳过），在历史记录中保留一个占位或跳过标记以解释话题跳跃。 @ S1  
  - A5.3 [用户] 选择「重新生成本轮」 → [系统] 重新调用 AI Model 基于相同上下文生成新内容，使用新内容替换当前轮的展示，同时保留旧版本用于内部调试或不保留（需在实现阶段决策）。 @ S1  
  - A5.4 [用户] 选择「结束演出」 → [系统] 结束当前会话，行为等同于 A2.3。 @ S1

- **US1-A6：网络中断或系统错误导致某轮生成失败**  
  - 条件：在生成某轮对话时出现网络错误、超时或系统异常。 ([Spec §Edge Cases 第 4 条])  
  - A6.1 在主流程步骤 6–7 中，某轮触发后生成失败 → [系统] 在对应轮次位置展示错误提示（例如「本轮生成失败：网络中断」），并在控制区提供「重试生成本轮」「跳过本轮」「结束演出」按钮。 @ S1  
  - A6.2 [用户] 选择「重试生成本轮」 → [系统] 再次请求 AI Model 生成该轮内容；如成功则正常展示并继续后续轮次。 @ S1  
  - A6.3 [用户] 选择「跳过本轮」或「结束演出」 → [系统] 行为同 A5.2 或 A2.3，并在历史记录中保留失败原因说明。 @ S1/S5

- **US1-A7：中断后继续演出**  
  - 条件：用户在一场进行中的演出中关闭或刷新页面。 ([Spec §Clarifications: 中断后继续演出], [Spec §FR-012])  
  - A7.1 会话进行到任意轮次时，[用户] 关闭或刷新页面 → [系统] 基于当前 DramaSession 与 Turn 数据持久化保存配置与已生成内容，将会话状态标记为「可继续」。 @ S1  
  - A7.2 下次用户再次进入 AI 情景剧功能入口或历史演出列表时 → [系统] 检测到存在进行中的会话，提示「检测到未完成的演出，是否从中断处继续？」并提供「继续」和「放弃」选项。 @ S1/S4  
  - A7.3 [用户] 选择「继续」 → [系统] 打开历史详情或主界面回放视图，从最后一个成功生成的 Turn 之后开始继续生成后续轮次。 @ S1/S5  
  - A7.4 [用户] 选择「放弃」 → [系统] 将会话状态标记为已结束（或放弃），保留已有历史可回看，但不允许继续生成新轮次。 @ S4/S5

---

### [US2] 利用 AI 生成新的 AI 演员角色设定 (Priority: P2)

#### Main Success Flow（通过对话生成并保存角色模板）

1. [用户] 从主界面或其他入口点击「创建新角色」 → [系统] 打开创建/编辑角色界面，提供角色目标描述输入区和结构化字段预览区（名称、背景、说话风格、行为原则等）。 @ S2 ([Spec §User Story 2], [Spec §FR-006], [Spec §Key Entities: RoleTemplate])
2. [用户] 在输入区用自然语言描述期望的角色（例如职业、性格、说话风格、与其他角色的关系） → [系统] 校验输入非空后启用「生成角色设定」操作。 @ S2 ([Spec §User Story 2 Acceptance 1])
3. [用户] 点击「生成角色设定」 → [系统] 调用 AI Model 使用预定义的角色生成 prompt 模板，生成包含名称建议、背景简介、说话风格、行为原则等字段的结构化角色 prompt 草稿，并填充到预览区。 @ S2 ([Spec §User Story 2 Acceptance 1], [Spec §FR-006])
4. [系统] 将生成的各字段以可编辑表单形式展示给用户，突出推荐名称和关键设定，同时保持原始自然语言描述可见。 @ S2 ([Spec §User Story 2 Acceptance 1])
5. [用户] 根据需要对生成的角色设定字段进行编辑和微调 → [系统] 即时更新预览，不触发重新生成。 @ S2
6. [用户] 点击「保存为角色模板」 → [系统] 校验必填字段后，将当前角色设定保存为新的 RoleTemplate，写入用户的角色库，并返回「保存成功」反馈。 @ S2/S3 ([Spec §User Story 2 Acceptance 2], [Spec §FR-005], [Spec §Key Entities: RoleTemplate])
7. [系统] 使新建角色模板可在情景剧创建界面和模板库中直接选取，用于后续演出配置。 @ S1/S3 ([Spec §User Story 2], [Spec §FR-005], [Spec §FR-009])

#### Alternate / Exception Flows

- **US2-A1：对生成结果不满意，重新生成**  
  - 条件：系统已生成一份角色 prompt 草稿。 ([Spec §User Story 2 Acceptance 3])  
  - A1.1 在主流程步骤 4 或 5，[用户] 觉得当前草稿不符合预期 → [用户] 点击「重新生成」或补充更多描述后点击重新生成。 @ S2  
  - A1.2 [系统] 基于最新描述重新调用 AI Model 生成新的角色 prompt 草稿，替换当前草稿展示；已保存的历史模板保持不变，不会被覆盖。 @ S2/S3 ([Spec §User Story 2 Acceptance 3])

- **US2-A2：生成内容触发安全策略或失败**  
  - 条件：AI Model 返回可能违规或不适宜的内容，或请求失败。 ([Spec §FR-010])  
  - A2.1 在主流程步骤 3，生成请求因内容安全被拦截 → [系统] 不展示具体违规内容，而是在界面上给出友好提示（例如「根据安全策略无法生成该角色设定，请尝试修改描述」），并保持原始描述不变。 @ S2  
  - A2.2 在主流程步骤 3，请求因网络或系统错误失败 → [系统] 显示错误原因并提供「重试」按钮，重试时重复调用生成逻辑。 @ S2  
  - A2.3 对于触发安全拦截或系统错误的请求，[用户] 可以选择修改描述后再次生成，流程返回到主流程步骤 2–3。 @ S2

- **US2-A3：从情景剧配置中快速创建新角色**  
  - 条件：用户在创建情景剧时发现角色库中没有合适角色。 ([Spec §User Story 2], [Spec §User Story 1 Acceptance 1])  
  - A3.1 在 US1 主流程步骤 3 中，[用户] 点击「AI 生成新角色」入口 → [系统] 在当前上下文中打开 S2（作为对话框或侧边面板），自动携带本场景的设定信息，以便 AI 更好生成角色。 @ S1/S2  
  - A3.2 [用户] 按 US2 主流程生成并保存新角色模板后 → [系统] 自动将该角色回填到当前情景剧的角色列表中，用户无需手动再次选择。 @ S1/S2

---

### [US3] 管理与复用场景/角色 Prompt 库 (Priority: P3)

#### Main Success Flow（查看、搜索与复用模板）

1. [用户] 打开「我的库」入口 → [系统] 显示模板库界面，默认展示当前用户的场景模板和角色模板列表，包含名称、标签、最近使用时间等关键信息。 @ S3 ([Spec §User Story 3], [Spec §FR-004–FR-005], [Spec §FR-007])
2. [系统] 以分页或滚动列表方式加载模板数据，如暂无任何模板则展示空状态提示与「创建场景」「创建角色」入口。 @ S3 ([Spec §User Story 3], [Spec §FR-007])
3. [用户] 在搜索栏输入名称关键字或选择一个或多个标签 → [系统] 根据用户输入过滤模板列表，仅展示匹配的场景或角色，并在界面上显示当前筛选条件。 @ S3 ([Spec §User Story 3 Acceptance 1], [Spec §FR-007])
4. [用户] 点击列表中的某个模板 → [系统] 打开模板预览面板，展示该场景或角色的详细内容（包括名称、完整 prompt、标签等）。 @ S3 ([Spec §User Story 3 Acceptance 1])
5. [用户] 在预览面板中选择「用于新演出」 → [系统] 将该场景/角色加入一个新的会话配置草稿（可视为跳转或返回到 S1 并预填配置），用户可继续添加其他模板或新建项后启动演出。 @ S3/S1 ([Spec §User Story 3], [Spec §FR-008–FR-009])

#### Alternate / Exception Flows

- **US3-A1：编辑模板**  
  - 条件：用户选中某个已有的场景或角色模板。 ([Spec §User Story 3 Acceptance 2])  
  - A1.1 在主流程步骤 4，[用户] 点击「编辑」 → [系统] 将模板内容以可编辑表单形式展示，允许修改名称、详细描述、标签及 prompt 文本。 @ S3  
  - A1.2 [用户] 编辑完成后点击「保存」 → [系统] 校验并保存修改；后续新创建的情景剧如果选用该模板，将使用更新后的内容，而已完成的历史演出所使用的内容保持不变（依赖 DramaSession 中的快照）。 @ S3/S5 ([Spec §User Story 3 Acceptance 2], [Spec §FR-008–FR-009])

- **US3-A2：删除模板而不影响历史演出记录**  
  - 条件：用户希望清理不再使用的模板。 ([Spec §User Story 3 Acceptance 3], [Spec §FR-004–FR-005], [Spec §FR-013])  
  - A2.1 在主流程步骤 4，[用户] 点击「删除」 → [系统] 弹出确认对话框，说明删除模板不会影响历史演出记录，仅会从模板列表中移除。 @ S3  
  - A2.2 [用户] 确认删除 → [系统] 从模板库中移除该场景/角色模板；历史演出列表和详情仍可以正常展示曾经使用该模板的会话内容。 @ S3/S4/S5

- **US3-A3：从历史演出配置快速复制与复用 prompt**  
  - 条件：用户希望基于某次历史演出快速创建新演出或在外部工具中复用 prompt。 ([Spec §Clarifications: 历史演出详情页逐轮回看], [Spec §FR-008–FR-009])  
  - A3.1 [用户] 在历史演出列表中选择某条记录 → [系统] 打开该场演出的历史详情界面，展示完整对话及本场使用的场景与角色快照。 @ S4/S5  
  - A3.2 [用户] 点击「一键复制本场使用的场景与角色 prompt」 → [系统] 将当前会话中使用的场景与每个角色的 prompt 组装为易读文本并复制到剪贴板，同时弹出复制成功提示。 @ S5  
  - A3.3 [用户] 选择「基于本场配置开新场」 → [系统] 根据该历史会话快照创建新的会话配置草稿，在 S1 中预填场景与角色设定，用户可做少量修改后启动新演出。 @ S5/S1

- **US3-A4：历史演出列表的查看与删除**  
  - 条件：用户希望查看所有历史演出并清理部分记录。 ([Spec §Clarifications: 历史演出记录保留与删除], [Spec §FR-011–FR-013])  
  - A4.1 [用户] 打开「历史演出」列表入口 → [系统] 在 S4 加载当前用户所有已保存的 DramaSession 记录，支持按状态或时间排序。 @ S4  
  - A4.2 [用户] 选中一条或多条历史演出并点击「删除」 → [系统] 弹出确认提示，说明删除后将无法再回看对应对话内容。 @ S4  
  - A4.3 [用户] 确认删除 → [系统] 从历史列表中移除对应记录，并从存储中清理该会话的对话内容；其他未删除演出及模板库不受影响。 @ S4  

## Low-Fidelity Prototypes

### S1 情景剧创建与演出主界面

- **Purpose**: 支持用户在单一界面内完成场景与角色配置、启动演出、观看多轮对话并进行基本播放控制。（Story: US1, US3）
- **Priority**: P1 Smoke（核心 MVP 界面）
- **Layout outline**:
  - **Header 区**: 产品标题、导航入口（如「我的库」「历史演出」）、当前用户信息。
  - **左侧：配置面板**:
    - 场景配置卡片：场景名称、标签、场景 prompt 文本框或选取控件，「新建场景」「从最近使用/收藏选择」按钮。
    - 角色配置卡片：角色列表（每个角色显示名称、头像、简要角色设定），「添加角色」「AI 生成新角色」按钮，角色顺序拖拽/调整控件。
    - 演出参数配置：总轮数输入、推荐上限提示、校验错误提示区。
  - **右侧：演出时间线**:
    - 对话区域：按照轮次垂直排列的对话气泡，每条包含角色头像、名称、轮次编号与生成时间。
    - 空状态：尚未开始演出时展示引导说明，提示用户先完成配置并点击「开始演出」。
  - **底部：控制条**:
    - 主操作按钮：「开始演出」「暂停/继续」「下一轮」「结束本场」。
    - 当前进度指示：已完成轮数 / 目标轮数，会话状态标签（进行中/暂停/已完成/可继续）。
- **Key interactive elements & states**:
  - **开始演出按钮**：在配置满足 FR-001 要求（1 个场景 + 2–6 个角色）前为禁用状态，悬浮提示说明缺失项。
  - **暂停/继续/下一轮/结束按钮**：根据会话状态动态启用/禁用，并在点击时于当前轮次结束后生效。
  - **错误提示区域**：用于显示 Edge Cases 中提到的网络错误、内容安全拦截、轮数超上限提示等。

```text
+---------------------------------------------------------------+
| Header: 产品标题 | 我的库 | 历史演出 | 用户头像               |
+---------------------------+-----------------------------------+
| 左侧：配置面板            | 右侧：演出时间线                 |
| - 场景配置卡              | - 空状态 / 多轮对话气泡列表      |
| - 角色配置列表            |   [角色头像] 名称  第N轮  时间   |
| - 演出参数（轮数等）      |   「跳过本轮」「重新生成」操作   |
+---------------------------+-----------------------------------+
| 底部：播放控制条：开始 | 暂停/继续 | 下一轮 | 结束本场 | 进度 |
+---------------------------------------------------------------+
```

### S2 创建/编辑角色（含 AI 生成）界面

- **Purpose**: 帮助不会写高质量 prompt 的用户，通过自然语言描述快速生成结构化角色设定，并保存为可复用模板。（Story: US2, US1）
- **Priority**: P2
- **Layout outline**:
  - **Header 区**: 标题「创建新角色」或「编辑角色」，返回/关闭按钮。
  - **左侧：需求输入区**: 大文本输入框供用户描述角色需求；下方有生成按钮与生成状态提示。
  - **右侧：角色设定草稿区**: 结构化表单字段（名称、背景、说话风格、行为原则、标签、头像等），支持手动编辑。
  - **底部操作区**: 「保存为角色模板」「重新生成」按钮，以及错误/安全提示区域。
- **Key interactive elements & states**:
  - 生成按钮在输入为空时禁用，生成中显示加载状态。
  - 保存按钮在必填字段缺失时禁用，并在 hover/点击时展示缺失原因。
  - 安全拦截时，用明显但非惊扰性的提示替代具体违规内容。

```text
+------------------------------------------------------+
| Header: 创建新角色  [返回]                          |
+----------------------+-------------------------------+
| 左：需求输入区       | 右：角色设定草稿（可编辑）    |
| - 文本输入框         | - 名称                        |
| - [生成角色设定]按钮 | - 背景简介                   |
| - 生成状态           | - 说话风格                   |
|                      | - 行为原则                   |
|                      | - 标签 / 头像                |
+----------------------+-------------------------------+
| 底部：重新生成 | 保存为角色模板 | 错误/安全提示区   |
+------------------------------------------------------+
```

### S3 模板库（场景 & 角色）界面

- **Purpose**: 让高频用户高效管理和复用场景/角色 prompt，避免每次从零配置，提升长期创作效率。（Story: US3）
- **Priority**: P3
- **Layout outline**:
  - 顶部搜索与筛选条：关键字输入框、标签多选、类型切换（场景 / 角色 / 全部）、排序选项（如最近使用）。
  - 左侧列表：展示模板条目（名称、类型、标签、最近使用时间、简短描述）。
  - 右侧详情面板：展示选中模板的完整内容与操作按钮（编辑、删除、用于新演出）。
- **Key interactive elements & states**:
  - 空列表状态下展示创建入口与简短说明。
  - 删除操作带确认对话框，强调不会影响历史演出记录。

```text
+------------------------------------------------------+
| 搜索: [关键字_______] [标签▼] [类型▼] [排序▼]      |
+----------------------+-------------------------------+
| 左：模板列表         | 右：模板详情                  |
| - [场景] 名称        | 标题 + 类型 + 标签           |
| - [角色] 名称        | 完整 prompt 文本             |
| ...                  | [编辑] [删除] [用于新演出]   |
+----------------------+-------------------------------+
```

### S4 历史演出列表界面

- **Purpose**: 让用户浏览和管理所有历史演出记录，可进入详情回看或删除不再需要的记录。（Story: US1, US3）
- **Priority**: P1 支持（与 P1 主流程紧密相关）
- **Layout outline**:
  - 顶部过滤区：按状态（进行中/已完成）、时间范围、关键字等筛选。
  - 列表区域：每行展示会话标题、场景名称、参与角色数量、状态、创建时间/最近更新时间、主要操作（回看、继续、删除）。
- **Key interactive elements & states**:
  - 当存在进行中的会话时，以醒目标签提示「可继续」。
  - 批量选择 + 删除能力（可选，视实现复杂度而定）。

```text
+------------------------------------------------------+
| 筛选: [状态▼] [时间▼] [关键字_______]              |
+------------------------------------------------------+
| [进行中] 标题       场景名   角色数  最近更新  操作 |
| [已完成] 标题       场景名   角色数  最近更新  操作 |
| ...                                                  |
+------------------------------------------------------+
| 底部: 分页 / 滚动加载                               |
+------------------------------------------------------+
```

### S5 历史演出详情 / 回看 & 继续界面

- **Purpose**: 支持用户逐轮回看某次演出的完整对话，并在会话未完成时从中断处继续；同时作为「一键复制本场使用的场景与角色 prompt」与「基于本场配置开新场」的主要入口。（Story: US1, US3）
- **Priority**: P1 支持
- **Layout outline**:
  - 顶部：会话标题、场景摘要、参与角色概览、会话状态标签（进行中/已完成/可继续）。
  - 左侧：按轮次展示的对话时间线，与 S1 的演出时间线样式一致，但处于只读状态。
  - 右侧：本场使用的场景 prompt 与角色 prompt 快照、一键复制按钮、「基于本场开新场」按钮。
  - 底部：如会话未完成，显示「从中断处继续演出」按钮。
- **Key interactive elements & states**:
  - 在会话进行中或可继续状态下，点击「从中断处继续」会将用户带回 S1 的演出视图并接续后续轮次。
  - 一键复制操作需要在视觉上给出明确反馈（例如 toast）。

```text
+------------------------------------------------------+
| 标题: 某某情景剧   [进行中/已完成]                  |
| 场景摘要 | 角色概览                                 |
+----------------------+-------------------------------+
| 左：对话时间线       | 右：配置快照与操作             |
| - 第1轮 [角色A] 文本 | 场景 prompt (只读)            |
| - 第2轮 [角色B] 文本 | 角色列表 + 各自 prompt       |
| ...                  | [一键复制本场 prompt]         |
|                      | [基于本场开新场]             |
+----------------------+-------------------------------+
| 底部：如未完成 → [从中断处继续演出] 按钮            |
+------------------------------------------------------+
```

## Open Questions, Assumptions & Spec Gaps

- [ASSUMPTION] S1 使用单一界面同时承载「配置」与「演出」视图，通过区域切换或状态切换实现，而不是分成独立的多步向导。（Story: US1, Screen: S1）
- [ASSUMPTION] S2（创建/编辑角色界面）既可以独立访问（从模板库或全局入口），也可以作为 S1 内的对话框/侧边栏调用，用于在配置情景剧时快速创建角色。（Story: US2, Screen: S1/S2/S3）
- [ASSUMPTION] 模板库 S3 默认按「最近使用时间」倒序排序，同时允许用户按名称或标签筛选；规范仅要求支持这些维度，未指定默认排序策略。（Story: US3, Screen: S3）
- [ASSUMPTION] 历史演出列表 S4 默认按「最近更新时间」倒序排序，以便用户快速找到最近的一场演出；规范未要求特定排序方式。（Story: US1, Screen: S4）
- [ASSUMPTION] 「一键复制本场使用的场景与角色 prompt」的主要入口位于 S5（历史详情页），并可选地在 S1 的进行中或已完成状态提供相同能力；规范未明确具体位置。（Story: US3, Screen: S1/S5）
- [ASSUMPTION] 当用户在历史列表中删除一条演出记录时，系统会物理删除对应的对话内容，而不是仅在 UI 层隐藏；规范仅说明「删除任意记录」，未说明底层存储策略。（Story: US3, Screen: S4）

- [NEEDS SPEC UPDATE] Edge Cases 第 3 条提到「当用户配置了超过推荐上限数量的角色（例如超过 6 个）或轮数（例如超过 20 轮）」时需要明确提示并确认，但 Functional Requirement FR-001 明确要求「至少 2 个、至多 6 个 AI 演员」。需要澄清：系统是否允许用户在确认后突破 6 个角色/20 轮的硬限制，还是应当在 UI 与后端均强制不超过该上限。（Story: US1, Flow step: US1-A4, Spec §FR-001 + §Edge Cases）
- [NEEDS SPEC UPDATE] Clarifications 指出「支持在下次进入时从中断处继续演出」，但未明确当存在多个未完成会话时的行为：是自动选择最近一场、让用户从列表中选择，还是限制同一时间只能有一个进行中的会话。（Story: US1, Flow step: US1-A7, Spec §Clarifications: Session 2025-11-23）
- [NEEDS SPEC UPDATE] 对于「跳过该轮」的行为，规范仅说明允许用户跳过，但未定义在历史详情页和导出/复制内容中是否保留该轮的占位及失败原因，可能影响后续数据结构设计与用户理解。（Story: US1, Flow step: US1-A5/US1-A6, Spec §Edge Cases）

## Coverage Summary

| User Story ID | Priority | Has Main Flow? | Has Alt/Exception? | Screens Used       | Notes                                                                                   |
|---------------|----------|----------------|--------------------|--------------------|-----------------------------------------------------------------------------------------|
| US1           | P1       | Yes            | Yes                | S1, S4, S5         | 涵盖从配置到完整演出、控制、历史列表与详情、恢复演出及错误处理；存在角色/轮数上限处理的规范空白。 |
| US2           | P2       | Yes            | Yes                | S2, S1, S3         | 涵盖从自然语言描述到生成角色草稿、编辑与保存模板以及与情景剧配置的衔接。                           |
| US3           | P3       | Yes            | Yes                | S3, S4, S5         | 涵盖模板库增删改查与复用、历史记录查看与删除以及基于历史配置快速开新场与一键复制。               |

- **Metrics**:
  - Total user stories: **3**
  - Stories with ≥1 main flow: **3**
  - Stories with alt/exception flows: **3**
  - Count of `[ASSUMPTION]` items: **6**
  - Count of `[NEEDS SPEC UPDATE]` items: **3**

- **Severity heuristic**:
  - **CRITICAL**: 无 — 所有 P1/P2/P3 用户故事均已有完整主成功流，且未发现阻碍理解整体产品行为的直接矛盾。
  - **HIGH**: 1 项 — 角色数量与轮数上限的处理方式在 FR-001 与 Edge Cases 之间存在潜在冲突，需要在进入技术规划前澄清是否允许突破上限以及如何统一前后端约束。（[NEEDS SPEC UPDATE] 第 1 条）
  - **MEDIUM**: 2 项 — 多个未完成会话时的「继续演出」选择策略，以及「跳过该轮」在数据结构与回看体验中的呈现方式，影响实现细节与用户预期，但不阻碍 P1 MVP 的基本可用性。（[NEEDS SPEC UPDATE] 第 2–3 条）
  - **LOW**: 若干与排序默认值、界面布局等相关的 [ASSUMPTION]，属于后续实现阶段可微调的交互与信息架构决策。


```

### 结果概要

- **已生成文件**: `specs/001-ai-drama-stage/interaction-design.md`，完整覆盖 US1–US3 的主成功流与异常/备选流，所有步骤均显式标注使用的 Screen ID（S1–S5）与对应 `spec.md` 的引用。  
- **低保真原型**: 为 S1–S5 分别给出了目的、优先级、布局区域划分和关键交互元素，并用 ASCII 线框图展示了页面结构，保持技术无关。  
- **假设与规范缺口**: 明确列出 **6 条 `[ASSUMPTION]`**（如单页承载配置+演出、模板和历史列表的默认排序、一键复制入口位置等）和 **3 条 `[NEEDS SPEC UPDATE]`**（尤其是「2–6 个演员」与「超过 6 个/20 轮」提示之间的潜在冲突、多未完成会话的继续策略、跳过轮次在历史中的表现）。  
- **覆盖与严重度**: `Coverage Summary` 中记录 3 个用户故事均有主流与异常流，统计了上述假设/缺口数量，并标记 **1 个 HIGH 级问题**（角色/轮数上限规则需要在进入 `/speckit.plan` 前澄清），建议先用更新规范或运行 `/speckit.clarify` 解决这些高优先级缺口，再继续技术规划。

